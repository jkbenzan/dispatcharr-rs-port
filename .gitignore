# Rust
/target
# Note: Cargo.lock is intentionally NOT ignored for binaries.
# Committing it ensures reproducible Docker builds.

# Frontend build output (built by `npm run build` in svelte-frontend/)
/dist

# Data & Secrets
.env
*.db
*.db-journal

# IDEs
.vscode/
.idea/
*.dump
*.sql

# Reference Projects (local copies, not part of this repo)
Dispatcharr/
Dispatcharr-main/
enhancedchannelmanager-main/
channelidentifiarr/

# Frontend build artifacts
svelte-frontend/node_modules/
svelte-frontend/.svelte-kit/

# Scratch / one-off files
channels.json
docker.log
docker_tail.txt
extracted_group.txt
extracted_profile.txt
extracted_stream.txt
read_log.py
replace_group.txt
scratch_notifications.txt
server_logs.txt
scratch/
build_output.log